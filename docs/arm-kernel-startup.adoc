= ARM32 Kernel Startup: From stext to start_kernel()
:toc: left
:toclevels: 4
:toc-title: Table of Contents
:sectnums:
:sectnumlevels: 4
:icons: font
:source-highlighter: rouge
:rouge-style: github
:imagesdir: images
:experimental:
:xrefstyle: short

[.lead]
This document continues directly from the previous guide on zImage decompression.
At that point, `__enter_kernel` in the compressed `head.S` transferred control
to `stext` — the very first instruction of `arch/arm/kernel/head.S` in the
uncompressed `vmlinux`.

This document covers everything that happens from that moment — CPU running
at physical `0x40208000`, MMU completely off — all the way to the first line
of C code in `start_kernel()` in `init/main.c`.

[NOTE]
====
*Prerequisite*: link:Debugging-linux-boot-process.adoc[Debugging Linux Boot
Process] — the companion document covering the zImage build pipeline and
self-decompression stage.

*Reference*: https://people.kernel.org/linusw/how-the-arm32-kernel-starts[How the
ARM32 kernel starts] — Linus Walleij's in-depth article that this document
draws from and expands.

*Source*: `arch/arm/kernel/head.S` and `arch/arm/kernel/head-common.S`
====

'''

== Handoff Point — Where We Came From

The previous document ended with `__enter_kernel` in the *compressed* bootstrap
loader (`arch/arm/boot/compressed/head.S`) executing:

[source,armasm]
----
__enter_kernel:
    mov     r0, #0          @ r0 must be 0
    mov     r1, r7          @ restore MachID saved at zImage entry
    mov     r2, r8          @ restore DTB pointer saved at zImage entry
    mov     pc, r4          @ jump to stext at ZRELADDR = 0x40208000
----

At the instant `pc = 0x40208000`, the CPU state is:

[cols="1,2,4", options="header", width="100%"]
|===
| Register | Value | Meaning

| `r0` | `0x00000000` | ARM boot ABI — must be zero
| `r1` | `0xFFFFFFFF` | Machine type ID (ignored by device-tree kernels)
| `r2` | `0x48000000` | Physical address of Device Tree Blob (DTB)
| `pc` | `0x40208000` | `stext` — first instruction in `arch/arm/kernel/head.S`
| MMU  | OFF | No virtual memory — every address is physical
| Caches | OFF | I-cache and D-cache disabled
| CPSR | SVC mode | IRQ and FIQ masked
|===

.Physical RAM state at stext entry and register ABI
image::../../images/ks-stext-entry.png[stext entry, 700, align=center]

The key difference between this `head.S` and the compressed `head.S` is that
this code was *linked* to execute at a virtual address (typically `0xC0208000`)
but is *physically* executing at `0x40208000`.  The earliest code must therefore
be written as position-independent assembly — it cannot use any linked address
until it has set up the MMU.

'''

== The Complete Startup Flow

Before diving into each phase, here is the complete sequence from `stext` to
`start_kernel()`:

.Complete ARM32 kernel startup flow from stext to start_kernel()
image::../../images/ks-startup-overview.png[Startup overview, 740, align=center]

[cols="1,2,3", options="header", width="100%"]
|===
| Phase | Symbol | Purpose

| 1 | `stext` | Entry point, initial register setup
| 2 | `__lookup_processor_type` | Identify CPU, find `proc_info` structure
| 3 | `__fixup_pv_table` | Calculate `PHYS_OFFSET`, patch P2V conversions
| 4 | `__create_page_tables` | Build initial section mappings in `swapper_pg_dir`
| 5 | `__v7_setup` (proc-v7.S) | CPU-specific cache/TLB setup, prepare SCTLR
| 6 | `__enable_mmu` / `__turn_mmu_on` | Activate MMU, flush pipeline
| 7 | `__mmap_switched` | Stack setup, BSS clear, branch to C
| 8 | `start_kernel()` | First fully C kernel code
|===

'''

== Virtual Memory Split — Where Will the Kernel Live?

Before examining the assembly, we must understand the virtual address space
layout that governs every decision in `head.S`.

=== PAGE_OFFSET — The Kernel/User Split

The kernel and userspace share the same 32-bit virtual address space.
`PAGE_OFFSET` defines the boundary:

.ARM32 virtual memory split options and typical use cases
image::../../images/ks-virt-phys-split.png[Virtual physical split, 700, align=center]

[source,kconfig]
----
config PAGE_OFFSET
    hex
    default 0x40000000 if VMSPLIT_1G   # kernel-heavy (routers, NAS)
    default 0x80000000 if VMSPLIT_2G   # balanced split
    default 0xB0000000 if VMSPLIT_3G_OPT
    default 0xC0000000                  # default — user-heavy (phones, tablets)
----

The overwhelming majority of ARM Linux systems use `PAGE_OFFSET = 0xC0000000`,
giving:

* *Userspace*: `0x00000000` – `0xBFFFFFFF` (3 GiB) — per-process virtual memory
* *Kernel space*: `0xC0000000` – `0xFFFFFFFF` (1 GiB) — always-mapped kernel memory

The kernel is *always mapped* in every process's page table.  When a userspace
process makes a system call, the CPU simply switches privilege level — no page
table swap is needed because the kernel was already present in the upper 1 GiB.
This makes kernel entry extremely fast.

=== KERNEL_RAM_VADDR — Where stext Is Linked

[source]
----
KERNEL_RAM_VADDR = PAGE_OFFSET + TEXT_OFFSET
                 = 0xC0000000  + 0x208000
                 = 0xC0208000    ← virtual address to which the kernel is linked
----

The kernel binary was compiled and linked assuming it will execute at
`0xC0208000`.  But right now, with the MMU off, it is physically at `0x40208000`.
This is the fundamental tension that the early startup code must resolve.

'''

== Phase 1: stext — CPU and Feature Detection

=== Entry Conditions Check

`head.S` begins with checks for two advanced features:

[source,armasm]
----
ENTRY(stext)
 THUMB(  adr   r9, BSYM(1f)   )   @ Thumb-2 interworking
 ARM(    mov   r9, r9          )   @ no-op in ARM mode
        mrc   p15, 0, r9, c0, c0  @ get processor ID (CPUID)
        bl    __lookup_processor_type  @ find proc_info in proc-v7.S etc.
        movs  r10, r5              @ r10 = proc_info ptr (0 = not found)
        beq   __error_p            @ BUG if processor not supported!
----

=== __lookup_processor_type

This function reads the CPU ID register (`CP15 c0`) and walks the `proc_info_list`
table embedded in the kernel to find the correct processor descriptor:

[source,armasm]
----
__lookup_processor_type:
    adr     r3, __lookup_processor_type_data
    ldmia   r3, {r4-r6}
    sub     r3, r3, r4          @ get offset between runtime and link
    add     r5, r5, r3          @ adjust __proc_info_begin to physical
    add     r6, r6, r3          @ adjust __proc_info_end to physical
1:  ldmia   r5, {r3, r4}        @ load value/mask pair from proc_info
    and     r4, r4, r9          @ mask CPU ID
    teq     r3, r4              @ does it match?
    beq     2f                  @ yes → found it
    add     r5, r5, #PROC_INFO_SZ
    cmp     r5, r6
    blo     1b
    mov     r5, #0              @ not found
2:  ret     lr
----

The matching entry (for Cortex-A15 in our QEMU session) is in
`arch/arm/mm/proc-v7.S` and contains:

[cols="1,3", options="header", width="90%"]
|===
| Field | Meaning

| `.long 0x410fc0f0` | CPUID match value for Cortex-A15
| `.long 0xff0ffff0` | CPUID mask
| `__v7_setup` | Function pointer for CPU-specific initialization
| `v7_processor_functions` | Cache management function pointers
| `v7_tlb_fns` | TLB operation function pointers
| `v7_user_fns` | User memory access function pointers
| `v7_cache_fns` | Cache operation function pointers
|===

=== __lookup_machine_type (non-DT boot) / Verify DTB (DT boot)

For device-tree boots the machine type validation is straightforward — the DTB
itself carries the compatibility string.  For legacy ATAG boots the code walks
the `__arch_info_begin` / `__arch_info_end` table similarly to `proc_info`.

'''

== Phase 2: Calculating PHYS_OFFSET and P2V Patching

This is one of the most important and subtle sections of `head.S`.  The kernel
must determine *where in physical RAM it is actually running* without being able
to rely on any compile-time constant.

=== The Problem

The kernel was linked to execute at `0xC0208000` (virtual).  It is physically
running at `0x40208000`.  The difference is `0x80000000`.  But this offset is
not known at compile time — the same kernel image must boot on hardware where
RAM starts at `0x10000000`, `0x40000000`, `0x80000000`, or anywhere else.

=== The Solution — Runtime Delta Calculation

.PHYS_OFFSET runtime calculation using the linked/runtime address delta
image::../../images/ks-p2v-delta.png[P2V delta, 700, align=center]

[source,armasm]
----
    adr     r3, 2f              @ r3 = RUNTIME addr of label 2: (physical, e.g. 0x40208xxx)
    ldmia   r3, {r4, r8}        @ r4 = LINKED addr of 2: (virtual 0xC0208xxx)
                                @ r8 = PAGE_OFFSET (e.g. 0xC0000000)
    sub     r4, r3, r4          @ r4 = delta = runtime − linked
                                @           = 0x40208xxx − 0xC0208xxx
                                @           = 0x80000000  (PHYS_OFFSET − PAGE_OFFSET)
    add     r8, r8, r4          @ r8 = PAGE_OFFSET + delta
                                @           = 0xC0000000 + 0x80000000 = 0x40000000
                                @           = PHYS_OFFSET  ✓

    2:    .long   .             @ compile-time address of this label (virtual)
          .long   PAGE_OFFSET   @ compile-time PAGE_OFFSET constant
----

[NOTE]
====
The `.long .` trick stores the *virtual* address the linker assigned to label
`2:` as a compile-time constant.  At runtime, `adr r3, 2f` gives the *physical*
address of `2:`.  Subtracting them gives the exact offset between where the
kernel is *linked* and where it is *actually running*.
====

=== CONFIG_ARM_PATCH_PHYS_VIRT — Patching Every P2V Conversion

.P2V instruction patching — every conversion site patched at boot
image::../../images/ks-p2v-patch.png[P2V patching, 700, align=center]

The kernel contains thousands of calls to `__virt_to_phys()` and
`__phys_to_virt()`.  Each one is compiled as an ARM `ADD` or `SUB` with an
*immediate operand* that encodes the P2V offset.  At link time this immediate
is a placeholder.  At boot, `__fixup_pv_table` patches every single one:

[source,armasm]
----
__fixup_pv_table:
    adr     r0, 1f
    ldmia   r0, {r3-r7}
    mvn     ip, #0
    subs    r3, r0, r3          @ compute offset to physical memory
    add     r4, r4, r3          @ adjust __pv_table_begin to physical addr
    add     r5, r5, r3          @ adjust __pv_table_end   to physical addr
    add     r6, r6, r3          @ adjust __pv_phys_pfn_offset
    add     r7, r7, r3          @ adjust __pv_offset
    mov     r0, r8, lsr #PAGE_SHIFT  @ PFN of PHYS_OFFSET
    str     r0, [r6]            @ save to __pv_phys_pfn_offset (used by MM later)
    ...
    b       __fixup_a_pv_table  @ loop: patch every ADD/SUB in .pv_table

1:  .long   .
    .long   __pv_table_begin
    .long   __pv_table_end
    .long   __pv_phys_pfn_offset
    .long   __pv_offset
----

After this loop, every `__virt_to_phys(x)` in the kernel correctly computes
`x + (PHYS_OFFSET - PAGE_OFFSET)` and every `__phys_to_virt(x)` correctly
computes `x - (PHYS_OFFSET - PAGE_OFFSET)`.

[cols="2,4,3", options="header", width="100%"]
|===
| Symbol | Before Patching | After Patching (our session)

| `__virt_to_phys` `ADD` immediate | placeholder `0x00000000` | `0x80000000`
| `__phys_to_virt` `SUB` immediate | placeholder `0x00000000` | `0x80000000`
| `__pv_phys_pfn_offset` | undefined | `0x40000` (PHYS_OFFSET >> PAGE_SHIFT)
| `__pv_offset` | undefined | `0x80000000`
|===

[TIP]
====
This patching is *also* performed every time a kernel module (`.ko`) is loaded,
because modules also contain a `.pv_table` section.  The same
`__fixup_a_pv_table` loop is re-used for module loading.
====

'''

== Phase 3: __create_page_tables — Building the Initial MMU Mappings

Before the MMU can be turned on, the kernel must create a minimal page table
that maps:

. The kernel code itself at its virtual address (`0xC0208000+`)
. An *identity mapping* covering the few instructions immediately after MMU
  activation (required for pipeline flush safety)
. The DTB region (so the kernel can read it after MMU is on)

=== swapper_pg_dir — The Initial Page Table Location

.swapper_pg_dir physical address calculation via pgtbl macro
image::../../images/ks-swapper-pg-dir.png[swapper_pg_dir, 700, align=center]

[source,armasm]
----
__create_page_tables:
    pgtbl   r4, r8              @ r4 = physical address of swapper_pg_dir

    @ The pgtbl macro computes:
    @   r4 = PHYS_OFFSET + TEXT_OFFSET - PG_DIR_SIZE
    @      = 0x40000000  + 0x208000    - 0x4000
    @      = 0x40204000
    @
    @ In virtual memory this will be at:
    @   swapper_pg_dir = PAGE_OFFSET + TEXT_OFFSET - PG_DIR_SIZE
    @                  = 0xC0000000  + 0x208000    - 0x4000
    @                  = 0xC0204000
----

[cols="2,2,3", options="header", width="90%"]
|===
| Symbol | Value | Meaning

| `PHYS_OFFSET` | `0x40000000` | Physical RAM base (computed in Phase 2)
| `TEXT_OFFSET` | `0x208000` | Offset of `stext` from RAM base
| `PG_DIR_SIZE` | `0x4000` | Classic MMU: 16 KiB; LPAE: 20 KiB (`0x5000`)
| `swapper_pg_dir` (phys) | `0x40204000` | Where initial page table is written
| `swapper_pg_dir` (virt) | `0xC0204000` | Where it will appear after MMU on
|===

=== Step 1: Zero the Entire Page Table

[source,armasm]
----
    mov     r0, r4          @ r0 = start of page table (0x40204000)
    mov     r3, #0
    add     r6, r0, #PG_DIR_SIZE  @ r6 = end of page table
1:  str     r3, [r0], #4    @ zero one word, advance
    str     r3, [r0], #4
    str     r3, [r0], #4
    str     r3, [r0], #4
    teq     r0, r6
    bne     1b              @ loop until all 4096 entries zeroed
----

This unrolled 4-word loop clears all 16,384 bytes (4,096 × 4-byte entries) of
`swapper_pg_dir` to zero — meaning "no mapping" for every virtual address.

=== ARM32 Page Table Format — Section Descriptors

The initial page table uses *section mappings* — each entry covers exactly 1 MiB.
This means:

* The 16 KiB page table has 4,096 entries (16,384 bytes ÷ 4 bytes/entry)
* Each entry describes 1 MiB of virtual address space
* 4,096 × 1 MiB = 4 GiB — the entire 32-bit address space

.ARM32 section descriptor format — bit field breakdown
image::../../images/ks-page-table-format.png[Page table format, 700, align=center]

[source]
----
 31          20 19 18 17 16 15  12 11 10  9  5 4  3  2 1 0
┌─────────────┬──┬──┬──┬──┬──────┬────┬───┬────┬──┬────┬────┐
│ Base[31:20] │NS│nG│ S│APX│TEX+CB│AP  │DOM│ P  │XN│ CB │ 10 │
└─────────────┴──┴──┴──┴──┴──────┴────┴───┴────┴──┴────┴────┘
                                                         ↑
                                                    Must be 0b10
                                                    = Section type
----

For the initial kernel mapping the entry is built as:

[source,armasm]
----
    orr     r6, r7, #PMD_TYPE_SECT | PMD_SECT_AP_WRITE | PMD_SECT_AP_READ
    orr     r6, r6, #PMD_FLAGS_CACHED          @ C=1 B=1
    @ r6 is now the template section descriptor for kernel sections
    @ bits [31:20] will be filled with the physical base address
----

=== Step 2: Map Kernel Sections (Virtual → Physical)

[source,armasm]
----
    @ r8  = PHYS_OFFSET (0x40000000)
    @ r10 = proc_info pointer (from __lookup_processor_type)
    @ r7  = section descriptor flags

    adr     r0, __turn_mmu_on_loc
    ldmia   r0, {r3, r5, r6}
    sub     r0, r0, r3          @ r0 = physical offset

    @ Map the kernel sections:
    @ virtual  0xC0200000–0xC02FFFFF  →  physical  0x40200000–0x402FFFFF
    add     r0, r4, #(KERNEL_RAM_VADDR >> 18)   @ PGD index for 0xC0200000
    orr     r3, r7, r8, lsr #(SECTION_SHIFT - PMD_ORDER)
    add     r3, r3, #( PHYS_OFFSET >> SECTION_SHIFT) << PMD_ORDER
1:  str     r3, [r0], #4        @ write section descriptor
    add     r3, r3, #1 << SECTION_SHIFT  @ next 1MB
    cmp     r0, r6
    bls     1b                  @ loop for all kernel sections
----

=== Step 3: Map Identity Sections (Physical = Virtual)

[source,armasm]
----
    @ Map identical: virtual 0x40200000 → physical 0x40200000
    @ This keeps PC valid immediately after "mcr p15,0,r0,c1,c0,0" (MMU on)

    adr     r0, __turn_mmu_on
    mov     r0, r0, lsr #SECTION_SHIFT   @ which 1MB section are we in?
    orr     r3, r7, r0, lsl #SECTION_SHIFT  @ section descriptor: phys = virt
    add     r0, r4, r0, lsl #PMD_ORDER   @ PGD index for identity entry
    str     r3, [r0]            @ write identity section descriptor
----

=== Step 4: Map the DTB

[source,armasm]
----
    @ Map 1 or 2 sections covering the DTB (passed in r2 = 0x48000000)
    mov     r3, r2, lsr #SECTION_SHIFT     @ DTB section index
    add     r0, r4, r3, lsl #PMD_ORDER     @ PGD index for DTB virtual addr
    rsb     r3, r3, #(PAGE_OFFSET >> (SECTION_SHIFT - PMD_ORDER))
    orr     r3, r7, r3, lsl #SECTION_SHIFT @ DTB section descriptor
    orr     r3, r3, #PMD_SECT_XN           @ mark execute-never
    str     r3, [r0], #4                   @ first section
    add     r3, r3, #1 << SECTION_SHIFT
    str     r3, [r0]                       @ second section (DTB may span two)
----

=== The Two Key Mappings

.Side-by-side: physical memory vs virtual mapping after __create_page_tables
image::../../images/ks-create-page-tables.png[Create page tables, 700, align=center]

[cols="2,2,2,3", options="header", width="100%"]
|===
| Mapping Type | Virtual Range | Physical Range | Purpose

| Kernel sections
| `0xC0200000` – `0xC02FFFFF`
| `0x40200000` – `0x402FFFFF`
| Where kernel will execute after MMU on (non-identity)

| Identity sections
| `0x40200000` – `0x402FFFFF`
| `0x40200000` – `0x402FFFFF`
| Pipeline flush safety — covers `__turn_mmu_on` + 3 NOPs

| DTB sections
| `~0xC8000000`
| `0x48000000`
| Kernel can read DTB in virtual memory after boot
|===

=== Linux Page Table Terminology (ARM32)

[cols="1,1,3", options="header", width="100%"]
|===
| Linux Name | ARM Hardware | Meaning

| PGD (Page Global Directory)
| First-level table (L1)
| 16 KiB; 4,096 entries; each covers 1 MiB. This IS the `swapper_pg_dir`.
  Written to `TTBR0`.

| PMD (Page Middle Directory)
| Same as PGD on classic MMU
| For classic ARM32: PGD and PMD are *folded* — same structure.
  On LPAE: PMD is a separate 4-entry table (64-bit entries, 2 MiB per section).

| PTE (Page Table Entry)
| Second-level entries
| Used for 4 KiB page mappings. The initial boot table does NOT use these —
  it uses section descriptors entirely.

| P4D, PUD | Not used on ARM32 | These Linux levels are *folded* (unused) on ARM32.
|===

[NOTE]
====
The initial page table uses *section* descriptors exclusively — no second-level
page tables are created.  Each 4-byte entry in `swapper_pg_dir` directly maps
a 1 MiB section.  This is intentional: it is the simplest possible mapping
sufficient to boot the kernel.  The full fine-grained page tables are built
later by `paging_init()` in `setup_arch()`.
====

'''

== Phase 4: CPU-Specific Setup — proc-v7.S

Before enabling the MMU, the CPU must be configured.  `stext` calls the
per-CPU initialization function via the `proc_info` pointer found in Phase 1:

[source,armasm]
----
    ldr     r12, [r10, #PROCINFO_INITFUNC]  @ r12 = __v7_setup offset
    add     r12, r12, r10                   @ r12 = physical addr of __v7_setup
    ret     r12                             @ jump, lr = addr of label 1:
                                            @            (which branches to __enable_mmu)
1:  b       __enable_mmu
----

For Cortex-A class CPUs, `__v7_setup` in `arch/arm/mm/proc-v7.S` performs:

[source,armasm]
----
__v7_setup:
    adr     r12, __v7_setup_stack   @ save registers to local stack
    stmia   r12, {r0-r5, r7, r9, r11, lr}

    bl      v7_flush_dcache_all     @ clean and invalidate all D-cache
    ldmia   r12, {r0-r5, r7, r9, r11, lr}

    mov     r10, #0
    mcr     p15, 0, r10, c7, c5, 0  @ invalidate I-cache + BTB
    dsb
    mcr     p15, 0, r10, c8, c7, 0  @ invalidate I+D TLBs (all)

    @ Configure TTBCR — split at PAGE_OFFSET
    @ For PAGE_OFFSET = 0xC0000000: T1SZ = 2 (TTBR1 covers top 1 GiB)
    mrc     p15, 0, r10, c2, c0, 2  @ read current TTBCR
    orr     r10, r10, #TTB_FLAGS_SMP
    mcr     p15, 0, r10, c2, c0, 2  @ write TTBCR

    @ Prepare SCTLR value in r0 — cache enables, branch prediction, MMU bit
    @ The M bit (MMU enable) is SET in this value but the register is NOT
    @ written yet — that happens in __turn_mmu_on
    mrc     p15, 0, r0, c1, c0, 0   @ read current SCTLR
    orr     r0, r0, #CR_M           @ set MMU enable bit
    orr     r0, r0, #CR_C           @ set D-cache enable bit
    orr     r0, r0, #CR_I           @ set I-cache enable bit
    ret     lr                      @ return to label 1: → __enable_mmu
----

'''

== Phase 5: __enable_mmu — Turning On the MMU

This is the most critical section of the entire boot sequence.
One wrong instruction here causes an immediate fault with no way to recover.

.MMU activation sequence and pipeline flush strategy
image::../../images/ks-enable-mmu.png[Enable MMU, 660, align=center]

=== The Complete __enable_mmu Sequence

[source,armasm]
----
__enable_mmu:
    @ r0  = SCTLR value to write (MMU+cache bits set)
    @ r4  = physical address of swapper_pg_dir (0x40204000)
    @ r13 = virtual address of __mmap_switched (to jump to after MMU on)

#ifndef CONFIG_CPU_ICACHE_DISABLE
    orr     r0, r0, #CR_I       @ enable I-cache
#endif

    @ Set Domain Access Register: all domains = MANAGER
    @ (bypasses permission checks during initial boot)
    mov     r5, #(domain_val(DOMAIN_USER,   DOMAIN_MANAGER) | \
                  domain_val(DOMAIN_KERNEL, DOMAIN_MANAGER) | \
                  domain_val(DOMAIN_TABLE,  DOMAIN_MANAGER) | \
                  domain_val(DOMAIN_IO,     DOMAIN_CLIENT))
    mcr     p15, 0, r5, c3, c0, 0   @ write DACR

    @ Load page table base address into TTBR0
    mcr     p15, 0, r4, c2, c0, 0   @ TTBR0 = swapper_pg_dir physical addr

    @ Final TLB invalidation
    mov     r5, #0
    mcr     p15, 0, r5, c8, c7, 0   @ invalidate all I+D TLBs
    mcr     p15, 0, r5, c7, c5, 6   @ invalidate BTB

    b       __turn_mmu_on

__turn_mmu_on:
    mov     r0, r0
    instr_sync                       @ ISB — instruction synchronisation barrier
    mcr     p15, 0, r0, c1, c0, 0   @ *** WRITE SCTLR — MMU IS NOW ON ***
    mrc     p15, 0, r3, c0, c0, 0   @ read MIDR (pipeline flush — 3 instructions)
    mov     r3, r3                   @ nop
    mov     r3, r3                   @ nop
    mov     pc, r13                  @ branch to virtual __mmap_switched
----

----
Dump first level pagetable:

(gdb) x/240 0x40204000
0x40204000:     0x00000c12      0x00100c12      0x00200c12      0x00300c12
0x40204010:     0x00400c12      0x00500c12      0x00600c12      0x00700c12
0x40204020:     0x00800c12      0x00900c12      0x00a00c12      0x00b00c12
0x40204030:     0x00c00c12      0x00d00c12      0x00e00c12      0x00f00c12
0x40204040:     0x01000c12      0x01100c12      0x01200c12      0x01300c12
0x40204050:     0x01400c12      0x01500c12      0x01600c12      0x01700c12
0x40204060:     0x01800c12      0x01900c12      0x01a00c12      0x01b00c12
0x40204070:     0x01c00c12      0x01d00c12      0x01e00c12      0x01f00c12
0x40204080:     0x02000c12      0x02100c12      0x02200c12      0x02300c12
0x40204090:     0x02400c12      0x02500c12      0x02600c12      0x02700c12
0x402040a0:     0x02800c12      0x02900c12      0x02a00c12      0x02b00c12
0x402040b0:     0x02c00c12      0x02d00c12      0x02e00c12      0x02f00c12
0x402040c0:     0x03000c12      0x03100c12      0x03200c12      0x03300c12
0x402040d0:     0x03400c12      0x03500c12      0x03600c12      0x03700c12
0x402040e0:     0x03800c12      0x03900c12      0x03a00c12      0x03b00c12
0x402040f0:     0x03c00c12      0x03d00c12      0x03e00c12      0x03f00c12
0x40204100:     0x04000c12      0x04100c12      0x04200c12      0x04300c12
0x40204110:     0x04400c12      0x04500c12      0x04600c12      0x04700c12
0x40204120:     0x04800c12      0x04900c12      0x04a00c12      0x04b00c12
0x40204130:     0x04c00c12      0x04d00c12      0x04e00c12      0x04f00c12
0x40204140:     0x05000c12      0x05100c12      0x05200c12      0x05300c12
0x40204150:     0x05400c12      0x05500c12      0x05600c12      0x05700c12
0x40204160:     0x05800c12      0x05900c12      0x05a00c12      0x05b00c12
0x40204170:     0x05c00c12      0x05d00c12      0x05e00c12      0x05f00c12
0x40204180:     0x06000c12      0x06100c12      0x06200c12      0x06300c12
0x40204190:     0x06400c12      0x06500c12      0x06600c12      0x06700c12
0x402041a0:     0x06800c12      0x06900c12      0x06a00c12      0x06b00c12
0x402041b0:     0x06c00c12      0x06d00c12      0x06e00c12      0x06f00c12
0x402041c0:     0x07000c12      0x07100c12      0x07200c12      0x07300c12
0x402041d0:     0x07400c12      0x07500c12      0x07600c12      0x07700c12
0x402041e0:     0x07800c12      0x07900c12      0x07a00c12      0x07b00c12
0x402041f0:     0x07c00c12      0x07d00c12      0x07e00c12      0x07f00c12
0x40204200:     0x08000c12      0x08100c12      0x08200c12      0x08300c12
0x40204210:     0x08400c12      0x08500c12      0x08600c12      0x08700c12
0x40204220:     0x08800c12      0x08900c12      0x08a00c12      0x08b00c12
0x40204230:     0x08c00c12      0x08d00c12      0x08e00c12      0x08f00c12
0x40204240:     0x09000c12      0x09100c12      0x09200c12      0x09300c12
0x40204250:     0x09400c12      0x09500c12      0x09600c12      0x09700c12
0x40204260:     0x09800c12      0x09900c12      0x09a00c12      0x09b00c12
0x40204270:     0x09c00c12      0x09d00c12      0x09e00c12      0x09f00c12
0x40204280:     0x0a000c12      0x0a100c12      0x0a200c12      0x0a300c12
0x40204290:     0x0a400c12      0x0a500c12      0x0a600c12      0x0a700c12
0x402042a0:     0x0a800c12      0x0a900c12      0x0aa00c12      0x0ab00c12
0x402042b0:     0x0ac00c12      0x0ad00c12      0x0ae00c12      0x0af00c12
0x402042c0:     0x0b000c12      0x0b100c12      0x0b200c12      0x0b300c12
0x402042d0:     0x0b400c12      0x0b500c12      0x0b600c12      0x0b700c12
0x402042e0:     0x0b800c12      0x0b900c12      0x0ba00c12      0x0bb00c12
0x402042f0:     0x0bc00c12      0x0bd00c12      0x0be00c12      0x0bf00c12
0x40204300:     0x0c000c12      0x0c100c12      0x0c200c12      0x0c300c12
0x40204310:     0x0c400c12      0x0c500c12      0x0c600c12      0x0c700c12
0x40204320:     0x0c800c12      0x0c900c12      0x0ca00c12      0x0cb00c12
0x40204330:     0x0cc00c12      0x0cd00c12      0x0ce00c12      0x0cf00c12
0x40204340:     0x0d000c12      0x0d100c12      0x0d200c12      0x0d300c12
--Type <RET> for more, q to quit, c to continue without paging--
0x40204350:     0x0d400c12      0x0d500c12      0x0d600c12      0x0d700c12
0x40204360:     0x0d800c12      0x0d900c12      0x0da00c12      0x0db00c12
0x40204370:     0x0dc00c12      0x0dd00c12      0x0de00c12      0x0df00c12
0x40204380:     0x0e000c12      0x0e100c12      0x0e200c12      0x0e300c12
0x40204390:     0x0e400c12      0x0e500c12      0x0e600c12      0x0e700c12
0x402043a0:     0x0e800c12      0x0e900c12      0x0ea00c12      0x0eb00c12
0x402043b0:     0x0ec00c12      0x0ed00c12      0x0ee00c12      0x0ef00c12
----

=== CP15 Registers Written

[cols="2,2,4", options="header", width="100%"]
|===
| Register | CP15 Address | What is Written

| DACR (Domain Access Control Register) | `c3, c0, 0`
| All domains set to MANAGER — bypasses permission checking during boot

| TTBR0 (Translation Table Base Register 0) | `c2, c0, 0`
| Physical address of `swapper_pg_dir` = `0x40204000`

| TTBCR (Translation Table Base Control Register) | `c2, c0, 2`
| TTBR0/TTBR1 split configuration (set during `__v7_setup`)

| SCTLR (System Control Register) | `c1, c0, 0`
| MMU enable (`M` bit), caches (`C`, `I` bits), branch prediction
|===

=== Why the 3 NOPs Are Critical — The Identity Mapping Bridge

.Identity mapping bridging physical execution to virtual execution
image::../../images/ks-identity-map.png[Identity map, 700, align=center]

When `mcr p15, 0, r0, c1, c0, 0` writes the SCTLR:

. The MMU is activated *instantly*
. But the ARM pipeline has already fetched the *next* few instructions
   using *physical* addresses
. Those physical addresses (`0x40208xxx`) must be valid virtual addresses too

This is exactly why `__create_page_tables` builds the identity mapping:

[cols="2,4", options="header", width="90%"]
|===
| Address | What Happens

| `0x40208xxx` (the 3 NOPs) | *Identity-mapped*: virt `0x40208xxx` → phys `0x40208xxx`
  → MMU translates correctly, no fault
| `r13 = 0xC0208xxx` (__mmap_switched) | *Kernel-mapped*: virt `0xC0208xxx` → phys `0x40208xxx`
  → `mov pc, r13` jumps to virtual kernel address — leaves identity region permanently
|===

[WARNING]
====
The identity mapping covers only one 1 MiB section (`0x40200000`–`0x402FFFFF`).
The branch to `__mmap_switched` *must* happen before the CPU wanders outside
this section.  The 3 NOPs ensure the pipeline is fully drained before `mov pc, r13`
executes.
====

'''

== Phase 6: __mmap_switched — Final C Runtime and start_kernel()

After `mov pc, r13`, the CPU is executing at a *virtual* address for the first
time.  We are now in `arch/arm/kernel/head-common.S`:

.Complete __mmap_switched sequence leading to start_kernel()
image::../../images/ks-mmap-switched.png[mmap switched, 700, align=center]

[source,armasm]
----
__mmap_switched:
    @ At this point:
    @   MMU ON — all addresses are virtual
    @   PC = 0xC0208xxx (virtual)
    @   r0 = 0, r1 = MachID, r2 = DTB physical addr, r9 = CPUID

    adr     r3, __mmap_switched_data  @ r3 = physical addr of data table

    ldmia   r3!, {r4, r5, r6, r7}    @ load: __data_loc, _sdata, __bss_start, _end

    @ Step 1: Copy .data if XIP kernel (ROM → RAM)
    cmp     r4, r5          @ __data_loc == _sdata? (no copy needed if same)
1:  cmpne   r5, r6          @ .data end reached?
    ldrne   fp, [r4], #4
    strne   fp, [r5], #4
    bne     1b

    @ Step 2: Zero .bss segment
    mov     fp, #0
1:  cmp     r6, r7
    strcc   fp, [r6], #4    @ zero one word
    bcc     1b

    @ Step 3: Load stack pointer
    ldmia   r3, {r4, r5, r6, r7, sp}

    @ Step 4: Save processor information for setup_arch()
    str     r9, [r4]        @ processor_id = CPUID
    str     r1, [r5]        @ __machine_arch_type = MachID
    str     r2, [r6]        @ __atags_pointer = DTB phys addr
    cmp     r7, #0          @ any further fixups?
    bicne   ip, ip, #PSR_ENDIAN_SET
    mcrne   p15, 0, ip, c1, c0, 0  @ set endianness if needed

    @ Step 5: Branch to start_kernel() — NEVER RETURNS
    b       start_kernel
----

=== Data Table __mmap_switched_data

[source,armasm]
----
    .align  2
    .type   __mmap_switched_data, %object
__mmap_switched_data:
    .long   __data_loc      @ source of .data (ROM for XIP, same as _sdata otherwise)
    .long   _sdata          @ virtual start of .data
    .long   __bss_start     @ start of .bss
    .long   _end            @ end of .bss
    .long   processor_id    @ &processor_id global variable
    .long   __machine_arch_type  @ &__machine_arch_type
    .long   __atags_pointer @ &__atags_pointer (DTB physical addr)
    .long   cr_alignment    @ &cr_alignment
    .long   init_thread_union + THREAD_SIZE  @ initial kernel stack pointer
----

[cols="2,3", options="header", width="90%"]
|===
| Symbol | Content after __mmap_switched

| `processor_id` | CPUID from `CP15 c0` (e.g. `0x410FC0F2` for Cortex-A15)
| `__machine_arch_type` | Machine ID from `r1` (or `0xFFFFFFFF` for DT boots)
| `__atags_pointer` | Physical address of DTB (`0x48000000`)
| `sp` | `init_thread_union + THREAD_SIZE` = top of init task's 8 KiB stack
|===

'''

== Virtual Memory Layout After MMU On

.Complete virtual address space layout after the MMU is activated
image::../../images/ks-virt-memory-map.png[Virtual memory map, 700, align=center]

[cols="2,2,3", options="header", width="100%"]
|===
| Virtual Address | Region | Contents / Notes

| `0x00000000` | Userspace base
| NULL pointer trap — first 16 KiB causes a fault on access

| `0x00004000`–`0xBFFFFFFF` | Userspace (3 GiB)
| Per-process virtual memory — fully isolated between processes

| `0xC0000000` | `PAGE_OFFSET`
| Kernel space starts here — always mapped, always present

| `0xC0204000`–`0xC0207FFF` | `swapper_pg_dir` (16 KiB)
| Initial page table — written during `__create_page_tables`,
  abandoned after `paging_init()` rebuilds the full tables

| `0xC0208000` | `stext` / `_text`
| First kernel instruction — `PAGE_OFFSET + TEXT_OFFSET`

| `0xC0208000`–`end` | Kernel image
| `.head.text`, `.text`, `.init.*`, `.data`, `.bss`

| `vmalloc` region | above kernel image
| Dynamic kernel memory mappings (modules, ioremap, etc.)

| `0xFFFF0000` | High vectors
| ARM exception vectors (on ARMv6+ with `HIVECS` enabled in SCTLR)
|===

=== Physical ↔ Virtual Translation After Boot

After P2V patching, every address conversion in the kernel uses:

[cols="2,3,2", options="header", width="90%"]
|===
| Direction | Formula | Example

| Physical → Virtual | `virt = phys − PHYS_OFFSET + PAGE_OFFSET`
| `0x40208000 − 0x40000000 + 0xC0000000 = 0xC0208000`

| Virtual → Physical | `phys = virt − PAGE_OFFSET + PHYS_OFFSET`
| `0xC0208000 − 0xC0000000 + 0x40000000 = 0x40208000`

| Shorthand | `virt = phys + 0x80000000` (our session) |
|===

'''

== What Happens in start_kernel()

`start_kernel()` in `init/main.c` is the first C function.  It inherits the
sparse initial page table.  Its first job is to replace it:

.start_kernel() overview
image::../../images/ks-start-kernel.png[start_kernel overview, 700, align=center]

[cols="2,4", options="header", width="100%"]
|===
| Function Call | Purpose

| `setup_arch(&cmd_line)` | Architecture setup — calls `paging_init()`
| `paging_init()` | Rebuilds *all* page tables — discards `swapper_pg_dir` mappings
| `setup_per_cpu_areas()` | Allocate per-CPU data
| `build_all_zonelists()` | Set up memory zones (ZONE_DMA, ZONE_NORMAL, etc.)
| `trap_init()` | Install final exception vector table
| `mm_init()` | Initialize memory allocator
| `sched_init()` | Initialize the process scheduler
| `init_IRQ()` | Set up IRQ controllers
| `time_init()` | Initialize system timers
| `rest_init()` | Launch kernel thread PID 1 (`/init`)
|===

[NOTE]
====
Once `paging_init()` runs, the identity mapping (`0x40200000` → `0x40200000`)
and the initial `swapper_pg_dir` entries are *completely replaced* by a full
fine-grained page table covering all of physical RAM.  The 1 MiB section
descriptors we built in `__create_page_tables` are gone, replaced by 4 KiB
page mappings with proper access permissions and memory attributes.
====

'''

== Debugging the Kernel Startup with GDB

=== Loading vmlinux Symbols

To trace from `stext` onwards, load the uncompressed `vmlinux` at `TEXT_OFFSET`:

[source]
----
(gdb) target remote :1234
(gdb) add-symbol-file arch/arm/boot/compressed/vmlinux 0x40010000
(gdb) add-symbol-file vmlinux 0x40208000
(gdb) hbreak stext
(gdb) continue
----

.GDB with vmlinux symbols loaded — stext and surrounding functions visible
image::../../images/gdb-vmlinux-symbols.png[GDB vmlinux symbols, 640, align=center]

=== Key Breakpoints for head.S Tracing

[cols="2,4", options="header", width="90%"]
|===
| Breakpoint | What to Inspect

| `break stext` | Entry state: `r0=0, r1=MachID, r2=DTB, pc=0x40208000`
| `break __lookup_processor_type` | CPU identification — `r9` = CPUID
| `break __create_page_tables` | Before: all zero; After: inspect `x/16wx 0x40204000`
| `break __enable_mmu` | `r4` = `swapper_pg_dir` addr, `r13` = `__mmap_switched` virt
| `break __turn_mmu_on` | Last instruction before MMU on — single-step carefully
| `break __mmap_switched` | First virtual-mode instruction — `sp` now set
| `break start_kernel` | First C code — `backtrace` should show only `start_kernel`
|===

=== Inspecting the Page Table

After `__create_page_tables` completes, inspect `swapper_pg_dir` directly:

[source]
----
(gdb) x/16wx 0x40204000          @ first 16 entries (0x00000000–0x0FFFFFFF virtual)
0x40204000: 0x00000000  0x00000000  ...    @ all zero (unmapped)

(gdb) x/wx 0x40204000 + 4*(0x402>>1)    @ entry for identity 0x40200000
0x40205008: 0x40200c1e   @ phys base 0x402, type=section(0b10), C=1,B=1,AP=11

(gdb) x/wx 0x40204000 + 4*(0xC02>>1)    @ entry for kernel 0xC0200000
(gdb) @ calculate: (0xC0200000 >> 20) * 4 = 0xC02 * 4 / 0x100 = offset into PGD
----

Decoding a section descriptor entry `0x40200c1e`:

[source]
----
0x40200c1e = 0100 0000 0010 0000 0000 1100 0001 1110
             ↑                   ↑    ↑         ↑  ↑
             [31:20]=0x402   [11:10]=AP=11 [C=1][B=1] [1:0]=0b10=Section
----

=== vmlinux Disassembly

.vmlinux disassembly showing head.S startup sequence from stext
image::../../images/vmlinux-disassembly.png[vmlinux disassembly, 640, align=center]

This snapshot shows the actual ARM instructions in the `stext` and nearby
functions as seen by `objdump`, confirming the assembly sequences described
in this document.

=== vmlinux Section Headers

.vmlinux ELF section headers showing .head.text, .text, .init.* layout
image::vmlinux-sections.png[vmlinux sections, 640, align=center]

The `.head.text` section always appears first — this is enforced by the linker
script (`arch/arm/kernel/vmlinux.lds.S`) using `FIRST_LOADABLE_SEGMENT`.
`stext` is the first symbol in `.head.text`.

'''

== Summary: Key Addresses and Symbols

[cols="3,2,1,4", options="header", width="100%"]
|===
| Symbol | Address | Space | Notes

| `stext` (physical) | `0x40208000` | Physical | Entry point from decompressor
| `stext` (virtual) | `0xC0208000` | Virtual | Linked address — active after MMU on
| `PAGE_OFFSET` | `0xC0000000` | Virtual | Kernel/user split boundary
| `PHYS_OFFSET` | `0x40000000` | Physical | Physical RAM base (computed at runtime)
| `TEXT_OFFSET` | `0x208000` | — | Gap between RAM base and `stext`
| `swapper_pg_dir` (phys) | `0x40204000` | Physical | Written by `__create_page_tables`
| `swapper_pg_dir` (virt) | `0xC0204000` | Virtual | Accessible after MMU on
| `PG_DIR_SIZE` | `0x4000` | — | 16 KiB — classic MMU; `0x5000` for LPAE
| `__turn_mmu_on` | `0x40208xxx` | Physical | Identity-mapped — safe post-MMU
| `__mmap_switched` | `0xC0208xxx` | Virtual | First virtual-mode code
| `DTB` (phys) | `0x48000000` | Physical | Passed in `r2`, mapped by page table
| P2V delta | `0x80000000` | — | `PHYS_OFFSET − PAGE_OFFSET` for this session
|===

[cols="1,2,4,2", options="header", width="100%"]
|===
| Phase | Symbol | Action | Key Registers

| 1 | `stext` | Entry, CPU detection, `__lookup_processor_type`
| `r9=CPUID, r10=proc_info`

| 2 | `__fixup_pv_table` | Compute `PHYS_OFFSET`, patch all P2V ADD/SUB immediates
| `r4=delta, r8=PHYS_OFFSET`

| 3 | `__create_page_tables` | Build `swapper_pg_dir`: zero, map kernel + identity + DTB
| `r4=swapper_pg_dir`

| 4 | `__v7_setup` | Flush caches+TLBs, configure TTBCR, prepare SCTLR value
| `r0=SCTLR value`

| 5 | `__enable_mmu` | Write DACR, TTBR0; final TLB flush; call `__turn_mmu_on`
| `r4=TTBR0`

| 6 | `__turn_mmu_on` | Write SCTLR (MMU ON); 3 NOPs; `mov pc, r13`
| `pc→0xC02xxxxx`

| 7 | `__mmap_switched` | Set stack, clear BSS, save IDs, `b start_kernel`
| `sp=init stack`

| 8 | `start_kernel()` | `paging_init()` rebuilds tables; full kernel init
| — (C code)
|===
